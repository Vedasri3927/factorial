#include <stdio.h>
int main() {
    			int n, i = 1;
    			long long factorial = 1;
    			      printf(“25331A05D6\n”);
 			printf("Enter a number: ");
    			scanf("%d", &n);
    			while (i <= n) {
        				factorial = factorial * i;
        				i++;
   			}
    			printf("Factorial = %lld", factorial);
    			return 0;
}

